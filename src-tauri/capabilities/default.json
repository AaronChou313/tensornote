{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum permissions for the bundled TensorNote main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "allow-platform-info",
    "allow-native-workspace",
    "allow-native-git",
    "allow-local-runtime"
  ]
}
