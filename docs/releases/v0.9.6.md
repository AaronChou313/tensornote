# TensorNote v0.9.6 — Tab Baseline Alignment

源码阶段日期：2026-08-31

v0.9.6 修复阅读编辑区标签标题的垂直基线问题。本阶段提交并推送源码，不创建 Git Tag 或 GitHub Release；最近正式 Release 仍为 v0.9.0。

## 修复

- 标签标题按钮显式采用 Flex 垂直居中，避免通用按钮基线规则将文字推向标签上沿。
- 保持 v0.9.5 的标签关闭、窗格关闭和历史导航交互设计不变。

## 验收

- `pnpm lint`、`pnpm build`、`git diff --check` 通过。
