# TensorNote v1.8.0

Project Experiment Preview B 增加安全的只读解析与 Workspace 索引。

- 解析 `tensornote-experiment` Fence 和 Manifest v1。
- 校验安全相对路径、环境继承、步骤 DAG、Runner、Preset 和输入文件。
- Workspace Session 索引合法与无效实验，诊断不会阻断其他笔记阅读。
- 知识库 validator、Skill reference 与可复制模板支持 Project Experiment。
- 解析与索引不执行进程、不访问网络、不安装依赖、不写入 Workspace。

兼容性：现有 Inline Lab、Workspace Schema v1 和 Provider API v1 保持不变。
