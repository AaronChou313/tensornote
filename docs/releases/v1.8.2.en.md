# TensorNote v1.8.2

This release delivers Project Experiment read-only parsing and indexing, and fixes an accidental third-party Rust lockfile version replacement in v1.8.1. The `blocking` dependency is restored to its valid locked version while the TensorNote package version is updated independently. The v1.8.0 and v1.8.1 tags remain as failed-release evidence.
