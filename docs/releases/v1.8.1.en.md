# TensorNote v1.8.1

This release delivers the Project Experiment read-only parser and index planned for v1.8.0, and fixes the untracked empty `assets/` directory that caused strict template validation to fail in a clean CI checkout. Scope and compatibility are unchanged; the v1.8.0 tag remains as failed-release evidence.
