# TensorNote v1.11.0

桌面版 Project Experiment 现在可运行结构化的 `python` 与 `python-module` 多脚本任务。运行前会展示环境、脚本 SHA-256、参数数组、步骤和输出范围；只有明确确认后才启动受管 Python 进程，不执行清单中的任意 Shell 字符串。

运行页实时显示逐步骤状态和限量日志，失败会阻断后续步骤，用户可取消、重新运行全部或从失败步骤继续。任务摘要保存在应用数据目录，异常退出后的运行任务会恢复为 `interrupted`；历史可手动清理。声明的产物可从桌面版在文件管理器中定位。
