# TensorNote v1.16.1

Fixes Desktop Outline clicks opening a “note not found” route. Desktop uses a HashRouter, where the old `#heading` links conflicted with application routing. Outline entries now invoke pane-scoped heading navigation without changing the application URL or another split pane.
