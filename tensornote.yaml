schemaVersion: 1

workspace:
  name: TensorNote Workspace Fixture
  description: 用于应用与知识库 Skill 验证的最小测试数据

content:
  root: fixtures/workspace/notes

assets:
  root: fixtures/workspace/assets

navigation:
  mode: filesystem

features:
  executable: false

environment:
  files: []

publishing:
  title: TensorNote Workspace Fixture
  description: 用于应用与知识库 Skill 验证的最小测试数据
  accent: '#4f8061'

extensions: {}
